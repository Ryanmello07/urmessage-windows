# build outputs
app/build/
[Bb]in/
[Oo]bj/
x64/
ARM64/
*.user
.vs/

# nuget
packages/

# generated by the XAML markup compiler INTO the source dir
app/src/App/XamlMetaDataProvider.cpp
app/src/App/XamlMetaDataProvider.idl

# tools/build-local.ps1 output
app/msbuild-local-*.log

# per-worktree app state (URMESSAGE_APP_ROOT), so concurrent worktrees do not
# share one prefs file or log. Never committed.
.localstate*/

# scratch: screenshots and harness output from verification runs
.verify*/

# The app icon (.ico) and the four brand faces ARE committed — they are needed
# at build time. See .gitattributes: they must never go through CRLF
# translation.

# brainstorming visual companion (local only)
.superpowers/
