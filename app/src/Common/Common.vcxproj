<?xml version="1.0" encoding="utf-8"?>
<!-- URmessage Common static library: identities, paths, logging, string
     conversion, thread guards, preferences, crash-dump channel probe.
     No pipe transport, no protocol, no SDK bootstrap — this product has no
     service to talk to.
     SPDX-License-Identifier: MPL-2.0 -->
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Debug|x64"><Configuration>Debug</Configuration><Platform>x64</Platform></ProjectConfiguration>
    <ProjectConfiguration Include="Release|x64"><Configuration>Release</Configuration><Platform>x64</Platform></ProjectConfiguration>
    <ProjectConfiguration Include="Debug|ARM64"><Configuration>Debug</Configuration><Platform>ARM64</Platform></ProjectConfiguration>
    <ProjectConfiguration Include="Release|ARM64"><Configuration>Release</Configuration><Platform>ARM64</Platform></ProjectConfiguration>
  </ItemGroup>

  <PropertyGroup Label="Globals">
    <VCProjectVersion>17.0</VCProjectVersion>
    <ProjectGuid>{C1D2E3F4-0001-4A5B-9C8D-000000000001}</ProjectGuid>
    <RootNamespace>UrmessageCommon</RootNamespace>
    <ConfigurationType>StaticLibrary</ConfigurationType>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />

  <PropertyGroup Label="Configuration">
    <UseDebugLibraries Condition="'$(Configuration)'=='Debug'">true</UseDebugLibraries>
    <UseDebugLibraries Condition="'$(Configuration)'=='Release'">false</UseDebugLibraries>
    <WholeProgramOptimization Condition="'$(Configuration)'=='Release'">true</WholeProgramOptimization>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />

  <ItemDefinitionGroup>
    <ClCompile>
      <!-- DYNAMIC CRT (MD). Common.lib links into the WinUI App, which requires
           the dynamic CRT (the Windows App SDK + WinUI are built /MD); a
           static-CRT (/MT) Common.lib triggers LNK2038 RuntimeLibrary-mismatch
           + LNK4098. The CRT flavor must match within, and across
           statically-linked, binaries. -->
      <RuntimeLibrary Condition="'$(Configuration)'=='Debug'">MultiThreadedDebugDLL</RuntimeLibrary>
      <RuntimeLibrary Condition="'$(Configuration)'=='Release'">MultiThreadedDLL</RuntimeLibrary>
      <Optimization Condition="'$(Configuration)'=='Release'">MaxSpeed</Optimization>
    </ClCompile>
  </ItemDefinitionGroup>

  <ItemGroup>
    <ClInclude Include="AppPrefs.h" />
    <ClInclude Include="CrashDumps.h" />
    <ClInclude Include="Ids.h" />
    <ClInclude Include="Log.h" />
    <ClInclude Include="Paths.h" />
    <ClInclude Include="Strings.h" />
    <ClInclude Include="ThreadGuard.h" />
    <ClInclude Include="Version.h" />
    <ClInclude Include="VersionGrammar.h" />
  </ItemGroup>
  <ItemGroup>
    <ClCompile Include="AppPrefs.cpp" />
    <ClCompile Include="CrashDumps.cpp" />
    <ClCompile Include="Log.cpp" />
    <ClCompile Include="Paths.cpp" />
    <ClCompile Include="Strings.cpp" />
    <ClCompile Include="ThreadGuard.cpp" />
  </ItemGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
